#%RAML 0.8

title: World Music API
baseUri: http://localhost:3333

/machines:
  get:
    headers:
      Abao-API-Key:
        type: string
        example: abcdef
    responses:
      200:
        body:
          application/json:
            schema: |
              [
                type: 'string'
                name: 'string'
              ]
            example: |
              { "type": "Kulu", "name": "Mike" }
